{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Flask Prototype",
      "type": "python",
      "request": "launch",
      "module": "backend.app.main",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "justMyCode": true,
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}

